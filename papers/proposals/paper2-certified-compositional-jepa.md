# Proposal — A Predictability Certificate for Equivariant World Models

> **Codename: paper2** (the flagship; absorbs the former "paper3" structure-of-prediction idea as its time/resolution axis). Direction approved 2026-06-02; merged 2026-06-02.
> **One-line thesis**: *Scale buys interpolation without guarantees; structure buys a **certificate**.* For an equivariant latent world model we give a single **3-axis predictability certificate** — over **configuration** (the generated group $\langle S\rangle$), **horizon** ($T$), and **resolution** ($\epsilon$) — whose certified region is exactly the **symmetry-protected, coarse-invariant, low-composition** corner, with boundary set by the group $\langle S\rangle$ **and** the dynamics' Lyapunov spectrum. No scale-only model has such a certificate on **any** axis.

---

## 1. The breakthrough claim (impact = a new *kind* of result, not a benchmark number)

Scaling lowers test error on the data manifold but issues **no statement about a specific unseen situation**. We claim a different *kind* of result — a **certificate**: a provable, computable region of situations the model is guaranteed to handle, *without further training or data*. Crucially the certificate spans **three orthogonal axes at once**:

- **Configuration** $w\in\langle S\rangle$ — generalize across the exponentially-large monoid generated by $k$ primitive symmetries (verified on the $k$ generators).
- **Horizon** $T$ — how far into the future the guarantee reaches.
- **Resolution** $\epsilon$ — at what level of detail.

The master finding: the certified region is the **coarse-invariant, symmetry-protected, low-$|w|$ corner**, and its boundary is set by $\langle S\rangle$ + the Lyapunov spectrum $\{\lambda_j\}$. This reframes the Bitter-Lesson debate from a *degree* contest (who needs less data — scale wins eventually) to a *kind* contest (who can issue a certificate — only structure can). **Impact = a tool for the field**: a single criterion that says *what* an equivariant world model can certifiably predict — and, read across domains, *why* celestial mechanics is predictable for millennia, weather dies at two weeks, and long-range forecasting systems can only ever be coarse.

> **✅ The "kind not degree" claim is now demonstrated (Step 51, 3 seeds, `experiments/step51_structure_vs_scale.py`).** Train on a $50°$ wedge of an $\mathrm{SO}(2)$ orbit, test around the full circle: the exactly-equivariant model's prediction error is **flat over the entire orbit** (out-of-wedge / in-wedge ratio $1.1\text{–}1.2$) — a certificate that holds from partial data by the identity $\mathrm{err}(R_\theta s)=\mathrm{err}(s)$. A non-equivariant MLP **scaled $84\times$ in parameters** ($4\mathrm{k}\to337\mathrm{k}$) buys real *interpolation* — its in-wedge error drops $30\text{–}166\times$ and even **beats** the equivariant model in-distribution — but its error still **climbs $170\text{–}2700\times$ out-of-wedge** and the largest MLP remains $10\text{–}155\times$ worse than a $56\times$-smaller equivariant model there. Scale buys interpolation; structure buys the certificate. The fair, honest nuance — *scale can win in-distribution* — is exactly why the contribution is the **certificate** (orbit-wide guarantee from partial data), not a per-point accuracy contest.

(Model of impact: a mechanism/theory paper à la JEPA, with a clean proof-of-principle at 1–2 GPU scale — **not** a scaled-SOTA benchmark. See §7.)

---

## 2. Setup and the master theorem (the three axes, unified)

**Setup.** Encoder $E:\mathcal X\to\mathcal Z$, action-conditioned predictor $f:\mathcal Z\times\mathcal A\to\mathcal Z$; a (mon­oid) element $g$ acts by $g\cdot x$ on states, by an **orthogonal** representation $\rho(g)$ on latents, by $\sigma(g)$ on actions. **Equivariance**: $E(g\cdot x)=\rho(g)E(x)$, $f(\rho(g)z,\sigma(g)a)=\rho(g)f(z,a)$. Let $f^{(T)}$ be the $T$-step rollout, $\mathrm{Err}_T(x;\vec a)=\lVert f^{(T)}(E(x),\vec a)-E(x^{\text{true}}_T)\rVert$ (or the closed-loop cost $J$).

**(Axis 1 — Configuration) Lemma (composition closure).** Equivariance on each generator $g_i\in S$ ⟹ equivariance on every word $w=g_{i_1}\cdots g_{i_m}\in\langle S\rangle$, since $\rho$ is a monoid homomorphism: $\rho(g_{i_1}\!\cdots g_{i_m})=\rho(g_{i_1})\cdots\rho(g_{i_m})$. *$k$ checks certify an exponential set.*

**Theorem A (exact certificate).** Under exact equivariance with orthogonal $\rho$: for every $w\in\langle S\rangle$, every $T$, every action sequence,
$$
\mathrm{Err}_T(w\cdot x;\sigma(w)\vec a)=\mathrm{Err}_T(x;\vec a),\qquad J(w\cdot x;w\cdot\text{goal})=J(x;\text{goal}).
$$
*Proof sketch.* $f^{(T)}$ equivariant by induction; orthogonality kills $\rho(w)$ inside the norm. (Multi-step, full-monoid lift of one-step [B].) $\square$

**(Axes 2+3 — Horizon × Resolution) Theorem B (spectral degradation — the load-bearing, honest result).** Promote the scalar Lipschitz $L$ to the predictor-Jacobian **spectrum** $\{e^{\lambda_j}\}$ along latent channels (e.g. the $\ell$-isotypic blocks). With measured residuals $\epsilon_{\max}$ (encoder, per generator) and $\delta$ (predictor),
$$
\big\lvert\mathrm{Err}_T(w\cdot x)-\mathrm{Err}_T(x)\big\rvert\ \le\ \sum_{\text{channels }j} c_j\,(m\,\epsilon_{\max}+T\,\delta)\,e^{\lambda_j T},\qquad m=|w|,
$$
so **channel $j$ is certified to horizon** $T_j(\epsilon)\sim \tfrac{1}{\lambda_j}\log\tfrac{1}{\epsilon}$ for $\lambda_j>0$, and $T_j=\infty$ for $\lambda_j\le 0$.

**Corollary (the certified region).** The set $\mathcal C=\{(w,T,\epsilon):|\Delta\mathrm{Err}|\le\epsilon\}$ is the **coarse-invariant, slow ($\lambda_j\le0$), low-$|w|$** corner; its boundary is $\langle S\rangle$ (config) $\times$ $\{\lambda_j\}$ (horizon/resolution). With **exact** equivariance ($\epsilon_{\max},\delta\sim10^{-6}$ by [A]) the config axis is unbounded and only the Lyapunov spectrum limits horizon×resolution — i.e. **a horizon × resolution trade-off**: $\text{horizon}\times\text{demanded-resolution}\lesssim$ const$(\{\lambda_j\})$. This is the formal content of "ultra-long forecasts must be coarse" (eclipses for millennia; weather at ~2 weeks; 推背图-style texts assert only regime-level change — structurally the *correct* move).

> **✅ Theorem B's staircase is measured (Step 52, 3 seeds, `experiments/step52_horizon_resolution.py`).** A learned predictor on a designed multi-Lyapunov system (conserved $\lambda{=}-\infty$ / contracting $\lambda{=}\ln0.75$ / neutral $\mathrm{SO}(2)$ rotor $\lambda{=}0$ / chaotic angle-doubling $\lambda{=}\ln2$) **recovers the chaotic Lyapunov exponent to $0.1\%$** ($\hat\lambda=0.691\text{–}0.692$ vs $\ln2=0.693$), and its per-channel certified horizon obeys $T_j(\epsilon)\sim\log(1/\epsilon)/\lambda_j$: the chaotic "fine detail" is certifiable only $3\text{–}10$ steps and its horizon grows only *logarithmically* as $\epsilon$ coarsens (measured slope $\mathrm{d}T/\mathrm{d}\log\epsilon=1.3\text{–}1.6\approx1/\lambda=1.44$), while the conserved/contracting (slow, invariant) channels stay certified for the *entire* $90$-step rollout ($\ge12\times$ longer). This is the predictability staircase — and the literal, measured form of the 推背图 / celestial-mechanics-vs-weather reading: **long horizon $\Rightarrow$ coarse + invariant only.**

---

## 3. The open hinge (headline **measured conjecture**, not hidden)

Theorems A–B hold for *whatever* channels are slow; what makes the symmetry axis and the time axis the **same** structure is a Noether-type bridge:

> **Conjecture (group ⇒ slow).** When the symmetry is a symmetry of the **dynamics** (not merely the encoder), the group-invariant ($\ell{=}0$) channels coincide with the conserved/slow ($\lambda_j\le0$) channels. Hence the symmetry that gives across-orbit (configuration) generalization is the *same* structure that gives long-horizon (time) predictability.

This is **not automatic** (group-invariant $\ne$ dynamically-conserved in general) — it is the paper's central, **falsifiable** claim, and we treat it as a *measured* conjecture: train the model, estimate per-channel $\lambda_j$, and test whether the $\ell{=}0$ block is in fact the slow/conserved one. Stating it openly (rather than assuming it) is the paper's sharpest, most honest edge.

**✅ Measured and confirmed — first direct test (Step 50, 3 seeds, `experiments/step50_noether_hinge.py`).** On a 2D $\mathrm{SO}(2)$-symmetric central-force system (conserved $E,L$ = invariant scalars; orbital phase = fast), a *learned* equivariant world-model (hand-rolled 2D Vector-Neuron autoencoder + latent predictor) spontaneously organizes the conserved quantities into its architecturally-invariant ($\ell{=}0$) block: regression $R^2$ for $(E,L)$ from the invariant block $=0.92\text{–}0.99$ vs $\le0.01$ from the equivariant ($\ell{=}1$) block, and the slowest mode the invariant block admits ($\approx0.01$, a conserved mode) is $\sim\!15\times$ slower than anything the equivariant block admits ($0.145$ = exactly the orbital phase velocity $2\sin(\omega\Delta t/2)$). The careful, **honest** form of the claim is the defensible direction — **slow $\subseteq$ invariant** (the conserved/slowest modes live *inside* the invariant subspace), *not* the false converse: a fast $|r|^2$ scalar is invariant yet oscillates, so invariant $\not\Rightarrow$ slow. The payoff is the certificate: the equivariant model's slow subspace **is** its invariant subspace, whose OOD group-action residual is $\sim\!10^{-16}$ *exactly and architecturally, for all of $\mathrm{SO}(2)$* — so the long-horizon-certifiable subspace is identifiable from structure for free and provably survives the whole group; a matched MLP also learns $E,L$ ($R^2{=}0.99$) but smears them across directions that drift by $\approx1.17$ under the same group action and carry no certificate. Guarded by `tests/test_step50_noether_hinge.py` (the architectural half needs no training).

**↗ Lift toward embodied/contact (Step 57, `experiments/step57_embodied_hinge.py`) — a partial lift and a principled 3D subtlety.** We cannot run this on the Step 24/43 object model — its latent is a *pure stack of $\ell{=}1$ vectors with no $\ell{=}0$ block*, so the hinge is not well-posed there — so we built the minimal faithful lift: two bodies in a shared 3D isotropic well with a **soft pairwise repulsion (contact)**, a hand-rolled 3D Vector-Neuron $\ell{=}0\oplus\ell{=}1$ latent. Result, honest: the **Noether-content half lifts** — the invariant ($\ell{=}0$) block recovers the conserved $(E,\text{contact-distance})$ at $R^2{=}0.86$ vs $0.05$ for the $\ell{=}1$ block, even with 3D contact — but the **clean containment slow $\subseteq$ invariant does *not* lift** (invariant slowest mode $0.024\approx$ equivariant $0.026$). This is principled, not a bug: **in 3D, angular momentum $L=\sum_i r_i\times v_i$ is a conserved $\ell{=}1$ *vector* (equivariant)**, so the equivariant block also carries a slow conserved mode. The clean 2D containment (where $L$ is a scalar) becomes, in 3D, $\text{slow}\subseteq(\text{invariant}\oplus\text{conserved-equivariant})$. **The hinge's Noether-content claim is dimension-robust; its clean-containment form is 2D-specific** — a real sharpening of §3's scope, reported as measured.

---

## 4. Why this is NEW (precise delta vs [A]/[B]/[C])

| | configuration | horizon | resolution | level |
|---|---|---|---|---|
| **[A]** | one $g$ | — | — | encoder residual $\le10^{-6}$ |
| **[B]** | one $g$ | $T{=}1$ | — | $\mathrm{relMSE}(gx)=\mathrm{relMSE}(x)$ |
| **[C]** | one $g$ | closed-loop | — | $J(gx)=J(x)$ |
| **Certificate (new)** | **all $\langle S\rangle$ (exp.)** | **any $T$** | **spectral $T_j(\epsilon)$** | **prediction + behavior** |

New content: **(i)** composition closure ($k$→exponential), **(ii)** multi-step propagation, **(iii)** the spectral horizon×resolution stratification, **(iv)** the group⇒slow Noether hinge. [A]/[B]/[C] are the $|w|{=}1$, single-resolution corners.

---

## 5. Building blocks (each maps to existing machinery)

1. **Master theorem** (§2–3) — *new*.
2. **Discovery** — the generating set $S$ is **discovered from interaction** (active inference / free energy), not hand-wired ⇒ the certificate is *earned*. Reuses Step 33/36 + 34/37. Defuses "you hand-designed the symmetry."
3. **Generation = certified orbit traversal** — "generating" a novel valid world-state/skill = applying $w\in\langle S\rangle$ in latent; the certificate guarantees validity. Generation ≡ generalization. Reuses Step 38.
4. **Compositional reach** — exponential certified set on Step 35 (few→many-body) + Step 19/24 (per-object $\mathrm{SE}(3)\times S_O$).
5. **Clean compositional testbed** — the **I Ching $\mathbb{Z}_2^6$ hexagram space**: 64 states $=2^6$, the 6 changing-lines = bit-flip generators, hexagram transitions = group action. A culturally-resonant, mathematically-clean analogue of BRo-JEPA's modular arithmetic (richer: $\mathbb{Z}_2^6$ vs $\mathbb{Z}_{10}$) — ideal minimal demo of the configuration axis.
6. **Horizon/resolution + honest degradation** — measure $\{\lambda_j\}$, the per-channel certified horizon, and the group⇒slow hinge under approximate symmetry (Step 30) and multi-step (Step 31); locate the certified region on the Step 22 symmetry×data phase diagram.

---

## 6. One experiment, three axes (the killer demo)

**World.** Object-centric 3D scene (Step 19/24): per-object $\mathrm{SE}(3)$ + object permutation $S_O$ → exponential $\langle S\rangle$. (Optional clean warm-up: the I Ching $\mathbb{Z}_2^6$ testbed for the config axis alone.)

**Certified model.** Exact-equivariant JEPA + equivariant planner; verify equivariance on the $k$ generators ([A]). Then, **with no further training**, read **one rollout dataset along all three axes**:
- **Config slice** (vary $|w|=1..M$): $\mathrm{Err}$ flat over $\langle S\rangle$ (certified) — overlay Thm-B bound.
- **Horizon slice** (vary $T=1..H$): per-channel decay; the $\ell{=}0$/coarse channels stay flat, fine channels decay $\sim e^{\lambda_j T}$.
- **Resolution slice** (vary $\epsilon$ / which irreps): the certified horizon $T_j(\epsilon)\sim\log(1/\epsilon)/\lambda_j$ trade-off, traced out.
- **Hinge test**: measure $\{\lambda_j\}$; check the $\ell{=}0$ block is the slow one (test §3).

**Headline plots.** (1) the certified region $\mathcal C$ in $(|w|,T,\epsilon)$; (2) **structure vs scale**: certified region (exponential, from $k$ generators) vs the region a **scaled non-equivariant baseline** + augmentation actually covers (polynomial in data) — and it carries **no certificate** on any axis.

**Discovery arm.** Repeat with $S$ discovered by an active-inference agent → earned certificate.

---

## 7. Honesty

- **Achievable (1–2 GPU):** the theorem(s) + a convincing small-scale proof-of-principle (the three slices + structure-vs-scale). Impact via the *idea + certificate + clean demo*.
- **Not claimed:** beating a scaled SOTA on a large real benchmark.
- **Genuine risks:** (a) real-world symmetry is approximate/weak → Theorem B *quantifies* the degradation and we now **measure it (Step 53)**: degradation is graceful (out-of-wedge error $\propto\epsilon_{\text{world}}$, corr $0.88\text{–}0.98$) and the certificate stays valuable up to a measured symmetry-content threshold ($\epsilon_{\text{world}}\approx0.01\text{–}0.06$) — exactly the "useful above a threshold" law, now quantitative. (b) the **group⇒slow hinge (§3)** — *now measured and confirmed on a controlled $\mathrm{SO}(2)$ system* (Step 50: slow $\subseteq$ invariant, $R^2{=}0.92\text{–}0.99$, certificate $10^{-16}$ vs MLP $1.17$); the remaining risk is whether it survives *approximate* symmetry on the embodied model — reported as measured, not assumed, and stated in its honest non-converse form.

### Attack points → contingencies
1. *"Just [B] restated."* → closure + multi-step + spectral stratification + the hinge are new; [B] is one corner.
2. *"Approximate symmetry kills it."* → the measured Theorem-B degradation (P4), not an unmodeled hole.
3. *"Toy / hand-wired group."* → discovery arm earns $S$; the I Ching testbed is a *clean* config-axis demo, the object world is the real one.
4. *"Hinge is hand-waved."* → it is stated as a falsifiable conjecture and **measured & confirmed** (Step 50: the $\ell{=}0$ block holds the conserved/slowest modes, $R^2{=}0.92\text{–}0.99$, and is exactly group-invariant where the MLP drifts) — with the honest non-converse ($|r|^2$ invariant-but-fast: slow $\subseteq$ invariant, not $=$).
5. *"Compounding rollout error."* → measure $\{\lambda_j\}$; with exact equivariance + $\lambda_j\le0$ channels the bound is tight; report the horizon where it holds.

---

## 8. Phased plan

- **P0 — Master theorem.** Write §2–3 rigorously (closure + Thm A + spectral Thm B + the §3 hinge as conjecture) + an equivariance-closure unit test (verify $\rho(g_1g_2)=\rho(g_1)\rho(g_2)$ and multi-step predictor equivariance). **✅ closure test done (Step 47, 2026-06-02, `tests/test_step47_certificate.py`)**: at init the equivariant model's relMSE deviates only **$1.18\times10^{-7}$** across composition words (Thm A holds architecturally, no training), post-word $\mathrm{SE}(3)$ residual $8.1\times10^{-6}$; the non-equivariant control deviates $20\%$ (the test discriminates). *(Tightening the proof prose of §2–3 still TODO.)*
- **P1 — Config axis.** I Ching $\mathbb{Z}_2^6$ clean demo + Step 19/24 object world: $\mathrm{Err}$ flat over $\langle S\rangle$; fit Thm-B. **✅ first result (Step 47 full, 3 seeds, `experiments/step47_certificate.py`)**: on the Step 19/24 object world the equivariant relMSE is **exactly $\times1.00$ flat over composition words $m=0..8$** (0.2625 throughout) vs the non-equivariant MLP's **$\times12.89$** blow-up; the predictor's Jacobian spectrum $\{\sigma_j\}$ is measured — **48/96 channels contractive** ($\sigma\le1$, certified long-horizon) vs 48 expansive (max $\sigma=49$, min $0.003$), a clean coarse/fine split = Thm-B's measured input. **✅ exponential demo done (Step 49, `experiments/step49_iching_certificate.py`)**: the I Ching $\mathbb{Z}_2^6$ testbed — 64 hexagrams, the 6 changing-lines as generators. Trained on the **6 single-line generators** ($+$ identity, $7$ of $64$ actions), the **equivariant** per-line predictor is certified over **all $2^6{=}64$ compositions** (relMSE $\sim\!10^{-33}$, machine zero, at *every* composition length; equivariance residual exactly $0$), while the non-equivariant MLP — same training actions — degrades monotonically with composition length ($1.6\times10^{-5}$ seen $\to 0.59$ at 6 flips). This is "$k$ generator checks $\Rightarrow 2^k$ certified set" made literal, with genuine learning (the equivariant model learns the per-line rules) and the honest MLP contrast.
- **P2 — Horizon × resolution axes.** Per-channel $\lambda_j$ + the $T_j(\epsilon)$ trade-off (reuse Step 31); **test the group⇒slow hinge**. **✅ hinge test done (Step 50, 3 seeds, `experiments/step50_noether_hinge.py` + `tests/test_step50_noether_hinge.py`)**: on a clean $\mathrm{SO}(2)$ central-force system the learned equivariant model's conserved/slowest modes live in its invariant block ($(E,L)$ recovery $R^2{=}0.92\text{–}0.99$ vs $\le0.01$; invariant slowest mode $\approx0.01 \ll 0.145$ equivariant), and that invariant=slow subspace is exactly group-invariant ($\sim10^{-16}$, $\forall\mathrm{SO}(2)$) where the MLP's slow directions drift ($\approx1.17$) — **slow $\subseteq$ invariant**, confirmed, with the honest non-converse ($|r|^2$ invariant-but-fast). **✅ the $T_j(\epsilon)$ staircase also done (Step 52, 3 seeds, `experiments/step52_horizon_resolution.py`)**: a learned predictor on a designed multi-Lyapunov system recovers the chaotic Lyapunov exponent to $0.1\%$ ($\hat\lambda=0.69$) and its certified horizon obeys $T_j(\epsilon)\sim\log(1/\epsilon)/\lambda_j$ (slope $1.3\text{–}1.6\approx1/\lambda$); chaotic detail certifiable $3\text{–}10$ steps, slow/invariant channels for the whole $90$-step rollout — the 推背图 predictability staircase, measured. **↗ embodied/contact lift attempted (Step 57)**: on a 3D two-body contact system the Noether-content half lifts (invariants $\to\ell{=}0$, $R^2{=}0.86$) but clean containment does not, because in 3D angular momentum is a conserved $\ell{=}1$ vector — a principled scope sharpening, not a failure. *(Remaining P2: a 3D-aware containment statement $\text{slow}\subseteq(\text{invariant}\oplus\text{conserved-equivariant})$; and the $T_j(\epsilon)$ curve under approximate symmetry.)*
- **P3 — Structure vs scale.** Scaled non-equivariant baseline; certified region vs data-covered region; no-certificate. **✅ done (Step 51, 3 seeds, `experiments/step51_structure_vs_scale.py`)**: wedge-train / full-circle-test on the $\mathrm{SO}(2)$ orbit — equivariant error flat over the whole orbit (ratio $1.1\text{–}1.2$ = certificate); an $84\times$-scaled MLP buys in-wedge interpolation ($30\text{–}166\times$, even beating the equivariant model in-distribution) but climbs $170\text{–}2700\times$ out-of-wedge and stays $10\text{–}155\times$ above the equivariant floor there. The killer "scale buys interpolation, structure buys the certificate" plot. *(Remaining P3: replicate on the discrete config axis — scale the MLP on the Step 49 I Ching $\mathbb{Z}_2^6$ task and show composition-length degradation is scale-invariant too.)*
- **P4 — Approximate-symmetry degradation.** Sweep $\epsilon$ (Step 30) × config × horizon; map onto the Step 22 phase boundary. **✅ done (Step 53, 3 seeds, `experiments/step53_approximate_symmetry.py`)**: breaking the world's $\mathrm{SO}(2)$ symmetry with an anisotropy knob $\beta$ — exact certificate at $\beta{=}0$ ($68\text{–}320\times$ better out-of-wedge than the MLP), **graceful** degradation (out-of-wedge error $\propto\epsilon_{\text{world}}$, corr $0.88\text{–}0.98$ = Theorem B's $\epsilon$ term, no cliff), and a **measured symmetry-content threshold** ($\epsilon_{\text{world}}\approx0.01\text{–}0.06$) beyond which structure stops paying — the Step-22 boundary made quantitative. *(Remaining: lift to the contact-rich embodied model.)*
- **P5 — Discovery + generation.** Discover $S$ (Step 33/36); generate = certified orbit-traverse an unseen composed task and execute closed-loop (Step 38/18). Writeup.

---

## 9. Working titles

- *A predictability certificate for equivariant world models: structure across configuration, time, and resolution*
- *Scale buys interpolation; structure buys a certificate*
- *什么能被认证地预测：等变世界模型的三轴可预测性证书*
